<p><%= link_to test.title, admin_test_path(test) %> (<%= t('tests.test.question', count: test.questions.count) %>)
  <%= link_to t('.links.edit'), edit_admin_test_path(test) %> |
  <%= link_to t('.links.destroy'), admin_test_path(test), data: { method: :delete } %>

  <%= button_to t('.buttons.start'), start_test_path(test) %>
</p>